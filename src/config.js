module.exports = {

    // name of the sqlite db
    dbFileName: ".db",

    // a static password to attach when registering a new user
    adminPassword: "change this",

    // min length for user password
    minPasswordLength: 8,

    // port to attach to
    port: 3000

};